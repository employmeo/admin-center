version: 0.0
os: linux
files:
  - source: /target/::JAR_FILE::
    destination: /home/ec2-user/drops/
hooks:
  AfterInstall:
    - location: deploy/stop.sh
      timeout: 300
      runas: root
    - location: deploy/deploy-latest.sh
      timeout: 300
      runas: root
    - location: deploy/start.sh
      timeout: 300
      runas: root
